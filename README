# Fyle Assignment

### Steps to run this code:
1. Clone this repo locally using  

    ```bash
    git clone https://github.com/amankrokx/fyleAssignment.git
    ```
2. Move into the cloned folder `fyleAssignment` using

    ```bash
    cd ./fyleAssignment
    ```
3. Start Node.Js script from root index.js file using

    ```bash
    node ./index.js
    ```

### Prerequisits:
- Node.Js version >= 16 required.

`Optional:`
- GitHub token can be added to `.env` file in order to increase rate limits.
- > PAT_TOKEN=\<Your-GitHub-Token>